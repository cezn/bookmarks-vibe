---
agent: playwright
---

Open 'http://localhost:5005' with playwright mcp. If you're redirected to login page, use these credentials:

- username: 'john.doe@test.com'
- password: 'Secret12#

You can navigate and do screenshots with playwright mcp if necessary.
Ensure `npm run dev -w  src/services/bookmarks-react-ui` is running which run Vite in watch mode.
