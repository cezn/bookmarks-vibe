services:
  redis:
    profiles:
      - infra
    image: redis/redis-stack:6.2.6-v17
    ports:
      - "0.0.0.0:6379:6379"
      - "0.0.0.0:8001:8001" # ui
    environment:
      REDIS_ARGS: "--appendonly yes"
    # depends_on:
    #   - otel-collector
    # logging: *logging
