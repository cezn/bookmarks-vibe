services:
  kibana:
    profiles:
      - elastic
    image: docker.elastic.co/kibana/kibana:9.3.0
    ports:
      - "0.0.0.0:5601:5601"
    depends_on:
      - elasticsearch
    environment:
      ELASTICSEARCH_HOSTS: "http://elasticsearch:9200"
